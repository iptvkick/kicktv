# Tasks: Spec 024 - Padronização Visual

## Fase 1: Frontend Ajustes (Frontend Engineer)
- `[x]` Em `src/routes/cliente/assinatura.tsx`: Substituir o `<h1>` atual pelo bloco `<header>` com o ícone `CreditCard`, seguindo a classe de estrutura especificada no `design.md`.
- `[x]` Em `src/routes/cliente/suporte.tsx`: Substituir o `<header>` atual pelo novo formato contendo o ícone `Headset`, seguindo a classe de estrutura especificada no `design.md`.
